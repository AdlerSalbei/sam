model ExternalApps {
  id                String    @id @default(cuid(2))
  name              String
  slug              String
  description       String
  icon              String
  imageSrc          String
  tags              String[]
  url               String
  team              String[]
}