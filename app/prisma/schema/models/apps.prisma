model externalApps {
  id                String    @id @default(cuid(2))
  name              String
  slug              String
  icon              String
  imageSrc          String
  tags              String
  url               String
}